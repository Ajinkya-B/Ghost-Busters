export class textTranscript{
    /**
     * A TextTranscript entity.
     * @param text
     * @param speaker
     */
    constructor(text, speaker) {
        this.text = text;
        this.speaker = speaker;
    }
}